@using NSchicht.Core.Konkret
@model List<AnwenderNachricht>
@{
    ViewData["Title"] = "AbsenderNachricht";
    Layout = "~/Views/Shared/_BenutzerLayout.cshtml";
}

    <div class="row">

              <div class="col-lg-12 grid-margin stretch-card">
                <div class="card">
                  <div class="card-body">
                    <h4 class="card-title">Ausgehende Nachrichten</h4>
                    <div class="table-responsive">
                      <table class="table table-striped">
                        <thead>
                          <tr>
                            <th> # </th>
                            <th> Gegenstand </th>
                            <th> Empfänger </th>
                            <th> Datum </th>
                            <th> Details </th>
                          </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                           {
                          <tr>
                            <td> @item.AnwenderNachrichtID </td>
                            <td> @item.Gegenstand </td>
                            <td> @item.EmpfaengerName </td>
                            <td> @(((DateTime)item.Datum).ToString("D")) </td>

                            <td><a href="/Benutzer/Nachricht/EmpfaengerNachrichtDetails/@item.AnwenderNachrichtID" class ="btn btn-outline-primary">Details</a></td>
                          </tr>                            
                           }
                        </tbody>
                      </table>
                    </div>
                  </div>
                </div>
              </div>
            </div>